/* eslint-disable no-unused-vars */
import Vue from 'vue';

import hitAlert from "./Alert.vue";
import hitButton from "./Button.vue";
import hitCol from "./Column.vue";
import hitContainer from "./Container.vue";
import hitRow from "./Row.vue";

const Components = {
  hitAlert,
  hitButton,
  hitCol,
  hitContainer,
  hitRow
};

export default Components;
